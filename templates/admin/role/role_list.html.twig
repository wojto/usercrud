{% extends 'admin/base.html.twig' %}

{% block content %}
    <div class="container-fluid">
        <div class="row">
            <h3 class="col-md-12">
                {{ 'role_list'|trans }}
            </h3>
        </div>

        <div class="table-responsive">
            <table class="table table-striped table-bordered table-hover">
                <thead>
                <tr>
                    <th class="col-md-5">
                        {{ 'role_id'|trans }}
                    </th>
                    <th class="col-md-5">
                        {{ 'role_name'|trans }}
                    </th>
                    <th class="col-md-2 text-center">
                        {{ 'role_add_date'|trans }}
                    </th>
                </tr>
                </thead>
                <tbody>
                {% for role in roles %}
                    <tr>
                        <td class="col-md-5">
                            {{ role.id }}
                        </td>
                        <td class="col-md-5">
                            {{ role.name }}
                        </td>
                        <td class="col-md-2 text-center">
                            {{ role.created|date('Y-m-d H:i') }}
                        </td>
                    </tr>
                {% else %}
                    <td colspan="3" class="col-md-12">{{ 'role_not_found'|trans }}</td>
                {% endfor %}
                </tbody>
                <tfoot>
                <tr>
                    <th colspan="3" class="col-md-12">{{ 'number_of_roles'|trans }}: {{ roles.getTotalItemCount }}</th>
                </tr>
                </tfoot>
            </table>
        </div>

        <div class="navigation">
            {{ knp_pagination_render(roles) }}
        </div>
    </div>
{% endblock %}
